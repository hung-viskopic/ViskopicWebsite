# Local development credentials only. Replace before any shared deployment.
DEV_ACCESS_KEY=local-viskopic-development-key
POSTGRES_PASSWORD=local-postgres-password
MINIO_ROOT_USER=viskopic-local
MINIO_ROOT_PASSWORD=local-storage-password
S3_REGION=us-east-1
